mkdir langchain-agent
cd langchain-agent
python3 -m venv .venv
python3 main.py

# (.venv) virtual environment similar to a shell
source .venv/bin/activate   # (on Windows: .venv\Scripts\activate)
pip install langchain openai python-dotenv

# Run from within (.venv)
pip install langchain langchain-openai

deactivate


# Github Marketplace Openai playground
pip install azure-ai-inference azure-core

# Telling Python: “These directories are modules. Allow imports from them.”
# may be needed for Virtualenv setups
__init__.py
